export const ZAMBIA_MARKET = {\n  country: 'Zambia',\n  countryCode: 'ZM',\n  currency: 'ZMW',\n  defaultCity: 'Lusaka',\n  priorityCities: ['Lusaka', 'Ndola', 'Kitwe', 'Livingstone', 'Kabwe', 'Chipata', 'Solwezi', 'Chingola', 'Mufulira'],\n} as const;\n\nexport interface Coordinates {\n  latitude: number;\n  longitude: number;\n  accuracy?: number | null;\n}\n\nexport function isValidCoordinates(value: Partial<Coordinates>): value is Coordinates {\n  return Number.isFinite(value.latitude)\n    && Number.isFinite(value.longitude)\n    && value.latitude >= -90\n    && value.latitude <= 90\n    && value.longitude >= -180\n    && value.longitude <= 180;\n}\n\nexport function normalizeCity(city?: string | null): string {\n  const normalized = city?.trim();\n  return normalized && normalized.length > 0 ? normalized : ZAMBIA_MARKET.defaultCity;\n}\n\nexport function formatMarketLocation(city?: string | null): string {\n  return normalizeCity(city) + ', ' + ZAMBIA_MARKET.country;\n}\n